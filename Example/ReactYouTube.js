// ReactImage.js
import { requireNativeComponent } from 'react-native';
// requireNativeComponent automatically resolves this to YouTubeManager
module.exports = requireNativeComponent('YouTubeManager', null);